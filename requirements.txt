# Core Django & Oscar
django>=4.2,<5.3
# django-oscar is installed in editable mode from src/
pillow>=6.0
django-extra-views>=0.13,<0.17
django-haystack>=3.0b1
django-treebeard>=4.7.0
Babel>=1.0,<3.0
purl>=0.7
phonenumbers
django-phonenumber-field>=4.0.0,<9.0.0
factory-boy>=3.3.1,<4.0.0
django-tables2>=2.3,<=2.7
django-widget-tweaks>=1.4.1
sorl-thumbnail>=12.10.0,<13.0.0
easy-thumbnails>=2.9,<2.11

# Search Backend (Whoosh is easier for free tiers, Solr/Elasticsearch recommended for scale)
Whoosh>=2.7,<2.8

# Deployment & Production
gunicorn
psycopg2-binary>=2.8,<2.10  # For PostgreSQL support
whitenoise>=6.7.0,<6.12     # For serving static files in production
django-environ>=0.4,<0.13   # For environment variable management
dj-database-url             # For parsing DATABASE_URL from hosting providers

# Payment gateways (Daraja / PayPal REST)
requests>=2.28,<3

# Oscar / Dukani live under src/ — editable install so `import oscar` works during
# Heroku slug compile (collectstatic runs before release; PYTHONPATH hooks do not persist).
-e .
